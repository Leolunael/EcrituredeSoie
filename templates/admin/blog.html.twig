{% extends 'admin/base.html.twig' %}

{% block title %}Gestion du blog{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Gestion des articles</h1>
            <div style="width: 180px;"></div> {# Espaceur pour centrer le titre #}
            <a href="{{ path('admin_index') }}" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Tableau de bord principal
            </a>
        </div>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endfor %}

        {% if blogs|length > 0 %}
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                    <tr>
                        <th style="padding-right: 50px;">Titre</th>
                        <th style="padding-right: 50px;">Date création</th>
                        <th style="padding-right: 50px;">Date modification</th>
                        <th style="padding-right: 50px;">À la une</th>
                        <th style="padding-right: 50px;">Commentaires</th>
                        <th>Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for blog in blogs %}
                        <tr>
                            <td style="padding-right: 50px;">{{ blog.titre }}</td>
                            <td style="padding-right: 50px;">{{ blog.dateCreation|date('d/m/Y H:i') }}</td>
                            <td style="padding-right: 50px;">
                                {% if blog.dateModification %}
                                    {{ blog.dateModification|date('d/m/Y H:i') }}
                                {% else %}
                                    -
                                {% endif %}
                            </td>
                            <td style="padding-right: 50px;">
                                {% if blog.aLaUne %}
                                    <span class="badge bg-success">Oui</span>
                                {% else %}
                                    <span class="badge bg-secondary">Non</span>
                                {% endif %}
                            </td>
                            <td style="padding-right: 50px;">
                                <a href="{{ path('app_admin_blogCommentaires') }}" class="btn btn-sm btn-secondary">
                                    Commentaires
                                </a>
                            </td>
                            <td>
                                <div class="btn-group" role="group">
                                    <a href="{{ path('app_admin_blog_modifier', {id: blog.id}) }}"
                                       class="btn btn-sm btn-warning" title="Modifier">
                                        ✏️
                                    </a>

                                    {% if not blog.aLaUne %}
                                        <a href="{{ path('app_admin_blog_mettre_a_la_une', {id: blog.id}) }}"
                                           class="btn btn-sm btn-info" title="Mettre à la une">
                                            ⭐
                                        </a>
                                    {% endif %}

                                    <form method="post"
                                          action="{{ path('app_admin_blog_supprimer', {id: blog.id}) }}"
                                          style="display: inline;"
                                          onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce texte ?');">
                                        <button type="submit" class="btn btn-sm btn-danger" title="Supprimer">
                                            🗑️
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>

            <div class="text-center mt-4">
                <a href="{{ path('app_admin_blog_nouveau') }}" class="btn btn-primary btn-lg">
                    <i class="fas fa-plus"></i> Nouvel article de blog
                </a>
            </div>
        {% else %}
            <div class="alert alert-info">
                Aucun article trouvé. <a href="{{ path('app_admin_blog_nouveau') }}">Créer le premier article</a>
            </div>
        {% endif %}
    </div>
{% endblock %}
