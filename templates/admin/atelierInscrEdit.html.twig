{% extends 'admin/base.html.twig' %}

{% block title %}Ecriture de Soie{% endblock %}

{% block body %}
    <div class="container-fluid mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1><i class="bi bi-pencil"></i> Modifier l'inscription</h1>
            <a href="{{ path('admin_atelier_show', {id: inscription.atelier.id}) }}" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Retour à l'atelier
            </a>
        </div>

        <div class="row">
            <div class="col-lg-8">
                <div class="card mb-3">
                    <div class="card-header bg-info text-white">
                        <h5 class="mb-0">Atelier : {{ inscription.atelier.titre }}</h5>
                    </div>
                    <div class="card-body">
                        <p><strong>Date :</strong> {{ inscription.atelier.dateAtelier|date('d/m/Y H:i') }}</p>
                        {% if inscription.atelier.lieu or inscription.atelier.rue or inscription.atelier.ville %}
                            <p>
                                <strong>Lieu :</strong>{% if inscription.atelier.lieu %}{{ inscription.atelier.lieu }}{% endif %}
                                                       {% if inscription.atelier.rue %} — {{ inscription.atelier.rue }}{% endif %}
                                                       {% if inscription.atelier.ville %} — {{ inscription.atelier.ville }}{% endif %}
                            </p>
                        {% endif %}
                    </div>
                </div>

                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h5 class="mb-0">Informations de l'inscrit</h5>
                    </div>
                    <div class="card-body">
                        {{ form_start(form) }}

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                {{ form_row(form.name) }}
                            </div>
                            <div class="col-md-6 mb-3">
                                {{ form_row(form.prenom) }}
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                {{ form_row(form.email) }}
                            </div>
                            <div class="col-md-6 mb-3">
                                {{ form_row(form.telephone) }}
                            </div>
                        </div>

                        <div class="mb-3">
                            {{ form_row(form.moyenPaiement) }}
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <a href="{{ path('admin_atelier_show', {id: inscription.atelier.id}) }}" class="btn btn-secondary">
                                <i class="bi bi-x-circle"></i> Annuler
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check-circle"></i> Enregistrer les modifications
                            </button>
                        </div>

                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
